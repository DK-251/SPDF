//! WASM bindings for SPDF core (used in Studio frontend).
//! Placeholder — full implementation in Week 6 (Day 33).

use wasm_bindgen::prelude::*;

#[wasm_bindgen]
pub fn version() -> String {
    "0.1.0".to_string()
}
